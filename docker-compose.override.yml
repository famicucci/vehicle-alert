services:
  backend:
    develop:
      watch:
        - action: sync
          path: ./vehicle-alert.backend/src
          target: /app/src
        - action: rebuild
          path: ./vehicle-alert.backend/package.json

  frontend:
    develop:
      watch:
        - action: sync
          path: ./vehicle-alert.frontend
          target: /app
          ignore:
            - node_modules/
            - .next/
        - action: rebuild
          path: ./vehicle-alert.frontend/package.json
